INIT (path, ERAD)

LOAD(PATH)

SI OK 
IF PRIVI????  && EARD /// LIB
ID EARD_PRESENT
	REPLACE READ SYMBOLS
	get datasize
ELSE !NOOK --> DUMMY
ENDIF
ENDIF

SI NOOK
LOAD (DUMMY)
ENDIF


read(void *c, edata_t energy_mj)

obs.read(c, void *c, energy_mj);
